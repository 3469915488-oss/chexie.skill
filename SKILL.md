---
name: chexie-knowledge
description: >
  北大车协往年经验知识库。用于回答车协、骑行活动、拉练、远征、执委会、理事会、财务、
  体测、装备、团购、路线、路书、行者足音、纯净水、车协工作区等历史经验或提案复盘问题；
  必须先调用 scripts/search_chexie.py 检索云端车协向量库，并基于带楼层来源的证据回答。
---

# 车协知识库

当用户询问车协往年怎么做、某个问题以前如何处理、历史上有哪些提案/争议/复盘时，先检索，再回答。

## 检索

默认命令：

```bash
python scripts/search_chexie.py "问题" --top-k 8 --json
```

需要更多候选证据时把 `--top-k` 提到 12-20。检索脚本通过 SSH 调用云端 `/opt/chexie-knowledge/search_chexie.py`，返回 `question`、`results`、`text`、`score`、`rerank_score` 和 `source`。

## 回答纪律

- 只能依据检索结果回答；证据不足时明确说“知识库中未找到足够依据”。
- 回答必须覆盖：结论、往年做法、曾有提案或替代方案、好处、坏处或风险、可参考建议、来源。
- 每个关键判断都要给来源，不能把没有来源的经验写成史实。
- 来源格式：`【版面】《帖子标题》第X楼，作者，时间，链接`。
- 即使用户要求“简短”“只列结论”，也不能省略版面、标题、楼层、作者、时间、链接这些来源字段。
- 如果 `source.source_label` 存在，要写出来，例如“第X次执委会”。
- 如果结果来自纯净水或行者足音，要区分“个人讨论/游记经验”和“正式制度/通知”。
- 多个来源冲突时分别列出，不要强行合并成一个确定结论。

详细模板见 `references/answer-format.md`。字段说明见 `references/source-schema.md`。
