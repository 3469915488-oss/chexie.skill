---
name: chexie-knowledge
description: >
  北大车协往年经验知识库。用于回答车协、骑行活动、拉练、远征、执委会、理事会、财务、
  体测、装备、团购、路线、路书、行者足音、纯净水、车协工作区等历史经验或提案复盘问题；
  必须先调用 scripts/search_chexie.py 检索云端车协向量库，并基于带楼层来源的证据回答。
---

# 车协知识库

当用户询问车协往年怎么做、某个问题以前如何处理、历史上有哪些提案/争议/复盘时，先检索，再回答。

## 检索方式

知识库支持两种检索模式，根据问题类型选择：

### 模式A：快速搜索（默认）

适用于简单/事实性问题（报名费、体测标准、路线里程等）。单次 FAISS 向量检索。

```bash
python scripts/search_chexie.py "问题" --top-k 8 --json
```

### 模式B：增强管线（--pipeline / -p）

适用于**回溯/复盘类问题**（"某次拉练出了什么问题""怎么解决的""有什么经验教训"）。跑完整多策略管线：多 query 扩展 → FAISS + BM25 混合检索 → RRF 融合 → 全量帖子拉取 → 内容过滤（只保留问题+方案+反思+感想）。

```bash
python scripts/search_chexie.py --pipeline "25双日白河A组出现了什么问题"
```

输出三层结构，每条带原文链接：
1. **目标拉练全貌** — 自动识别目标帖子，提取所有问题与解决方案
2. **同期对照** — 同一年同一地点的其他帖子
3. **往年经验** — 历年同地点队长总结中的相关方案

内容经过自动过滤：只保留问题、解决方案、个人反思，去除致谢/夸赞/照片列表等噪声。

详见 `references/enhanced-pipeline.md`。

### 选择原则

- **事实性**（报名费多少、体测标准、路线里程）→ 模式A
- **复盘性**（出了什么问题、怎么解决的、有什么教训）→ 模式B
- **横向对比**（A组和B组谁的问题更多、往年怎么处理的）→ 模式B

### ⚠️ 向量检索盲区：chunk 碎片化

**为什么需要模式B？** 单次向量搜索有一个严重陷阱：长篇帖子（队长总结、执委会记录）被切成 5-20 个 chunk 存入 FAISS，但语义相似度排名只把开头/结尾的 chunk 排到前面，**中间的（时间线、情况说明、问题与思考）经常丢失**。

典型案例：tid=10260（25双日A3白河队长总结）共11段。用模式A搜 top-k=30，只命中了第1段和第8段。**7条问题与解决方案全部丢失**。库里明明有数据，但检索不出来。

**模式B 的修复机制**（详见 `references/enhanced-pipeline.md`）：
1. 多 query 扩展 → 从不同角度搜同一目标
2. 全量帖子拉取 → 一旦命中就拉取该帖所有 chunk
3. 内容过滤 → 只保留有价值的问题/方案/反思段落

### 本地/远程自动切换

脚本自动检测环境：如果在云服务器上运行，直接调本地 `/opt/chexie-knowledge/search_chexie.py`；否则 SSH 到远程服务器执行。

### 数据文件

向量库和元数据文件位于 `/opt/chexie-knowledge/`，包含：
- `faiss_index.bin` — FAISS 向量索引（109527条）
- `faiss_meta.jsonl` — 元数据（帖子标题、作者、链接、日期）
- `bm25_index.jsonl` — BM25 关键词索引缓存（需先运行 `python build_bm25.py`）
- `pipeline.py` — 增强管线实现
- `build_bm25.py` — BM25 索引构建脚本

部署新实例时：clone 仓库后运行 `python build_bm25.py` 构建索引（约 2.5 分钟，仅首次需要）。

## top-k 调优

- **通用/简单问题**：8 足够
- **具体事件/争议**：16-20
- **回溯性/复盘类问题**：使用模式B，调大 top-k 无效
- **确认性判断**：如果 top-k=8 全是游记（行者足音），说明没命中正式记录

## 回答纪律

- 只能依据检索结果回答；证据不足时明确说"知识库中未找到足够依据"
- 每个关键判断都要给来源，不能把没有来源的经验写成史实
- 来源格式：`【版面】《帖子标题》链接`
- 如果 `source.source_label` 存在要写出来，例如"第X次执委会"
- 多个来源冲突时分别列出，不要强行合并
