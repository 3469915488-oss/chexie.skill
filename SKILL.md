---
name: chexie-knowledge
description: >
  北大车协往年经验知识库。检索 BBS 工作区、行者足音、纯净水三大版块的
  10 万+ 帖子，基于有来源、可回查的论坛记录回答车协历史经验、拉练复盘、
  执委会记录、远征流程、装备团购等问题。
---

# 车协知识库

当用户询问车协往年怎么做、某个问题以前如何处理、历史上有哪些提案/争议/复盘时，
**必须先检索**，再基于检索结果回答。

## 两种检索模式

### 模式A：快速搜索（默认）

适用于**事实性问题**（报名费、体测标准、路线里程、装备价格等）。
单次 FAISS 向量检索，快速精准。

```bash
python scripts/search_chexie.py "问题" --top-k 8 --json
```

### 模式B：深度复盘管线（--pipeline）

适用于**回溯性问题**（"某次拉练出了什么问题""怎么解决的""有什么经验教训"）。
多 query 扩展 → FAISS + BM25 混合检索 → RRF 融合 → 全量帖子拉取 → 内容过滤。

````bash
python scripts/search_chexie.py --pipeline "25双日白河A组出现了什么问题"
````

输出**三层结构**：
1. **目标拉练全貌** — 自动识别目标帖子，提取所有问题+解决方案+个人思考
2. **同期对照** — 同一年同一地点的其他队长总结，带链接和摘要
3. **往年经验** — 历史同地点队长总结中的相关方案

每条结果都带原文链接。内容过滤自动去除致谢、个人夸赞、照片列表等噪声，
只保留问题描述、解决方案和反思。

### 选择原则

| 问题类型 | 使用模式 | 原因 |
|----------|----------|------|
| "报名费多少""体测标准""路线里程" | A（快速搜索） | 事实性查询，单段即可回答 |
| "出了什么问题""怎么解决的""有什么教训" | B（深度管线） | 需要全量帖子+跨段聚合 |
| "A组和B组谁的问题更多""往年怎么处理的" | B（深度管线） | 需要横向对比多个帖子 |

### top-k 调优

| 场景 | top-k |
|------|-------|
| 简单事实查询 | 8 |
| 具体路线/事件/争议 | 12-16 |
| 回溯复盘/多方案比较 | 16-20 |
| 确认性判断（结果疑似偏差时加 k 重试） | 12+ |

## ⚠️ 向量检索盲区（chunk 断裂问题）

**为什么需要模式B？** 单次向量搜索有一个严重陷阱：
长篇帖子（队长总结、执委会记录）被切成 5-20 个 chunk 存入 FAISS，
但语义相似度排名只把开头/结尾的 chunk 排到前面，
**中间的（时间线、情况说明、问题与思考）经常丢失**。

典型案例：tid=10260（25双日A3白河队长总结）共11段。
用模式A搜 top-k=30，只命中了第1段和第8段。
**7条问题与解决方案全部丢失**。库里明明有数据，但检索不出来。

**诊断信号**：
- 搜索结果的同一帖子命中 chunk 数远少于 chunk_count
- 命中的片段是开头/结尾/致谢，没有中间分析段落
- 提问"出了什么问题"但答案只有通知/游记

**模式B 的修复机制**：多 query 扩展 + 全量帖子拉取 →
保证目标帖子的所有段落都被检索到，不受 chunk 分裂影响。

## 管线内容过滤规则

`--pipeline` 模式下，输出只保留以下内容：

**保留**：
- 问题描述（"问题与思考""情况说明""出现的问题"等章节）
- 解决方案（"解决方案""解决"等）
- 个人反思（"个人思考""反思""经验教训"等）
- 关键元数据（人员组成、路线、时间线、工作记录）
- 同期和历史的对照帖子链接

**过滤**：
- 致谢段落（"感谢XX辛苦啦""夸！""祝一切顺利"等）
- 个人夸赞（"超级靠谱""超级棒""期待下次一起骑车"等）
- 照片列表（"于四海坡顶""于百里画廊"等）
- 纯情绪化内容（"qwq""大家好好～夸～"等）

**chunk 边界残留**：FAISS 分段的固有问题，一句话被切成两段的
情况无法完全消除。如果输出中看到重复的半截句，属于已知边界，
不影响内容完整性。

## 回答纪律

- 只能依据检索结果回答；证据不足时明确说"知识库中未找到足够依据"
- 回答必须覆盖：结论、往年做法、曾有提案或替代方案、利弊、可参考建议、来源
- **每个关键判断都要给来源，不能把没有来源的经验写成史实**
- **来源格式**：`【版面】《帖子标题》作者 / 时间 / 链接`
  - 链接必须是**可直接点击回查的完整 URL**，不能省略
  - 从 `url` 字段直接取整条帖子的链接
  - 如果有楼层号，在链接后加 `#第X楼` 方便定位
- **举例**：
  ```
  【车协工作区】《25双日A3白河队长总结》烟景 / 2025-05-05
  https://www.chexie.net/bbs/content/index.php?bid=1&tid=10260
  ```
- 如果 `source.source_label` 存在，要写出来，例如"第X次执委会"
- 如果结果来自纯净水或行者足音，要区分"个人讨论/游记经验"和"正式制度/通知"
- 多个来源冲突时分别列出，不要强行合并

详细模板见 `references/answer-format.md`。字段说明见 `references/source-schema.md`。

## 首次使用准备

数据文件（faiss_index.bin + faiss_meta.jsonl）需部署到远程服务器
`/opt/chexie-knowledge/` 目录下。从 GitHub Releases 下载：

```bash
wget https://github.com/3469915488-oss/chexie.skill/releases/download/v2.0.0-pipeline/chexie_data.tar.gz
tar -xzf chexie_data.tar.gz -C /opt/chexie-knowledge/
# 可选：构建 BM25 索引（约 2.5 分钟，仅首次）
python /opt/chexie-knowledge/build_bm25.py
```
